# Python
__pycache__/
*.pyc
*.pyo

# venv (created by vendor/venv-jax / vendor/venv-torch install scripts)
.venv/
vendor/venv-jax/.venv/
vendor/venv-torch/.venv/

# Compiled kernels — platform/arch specific, rebuild on target (see vendor/FlashRT/README.md)
# All .so files are git-ignored. Use the provided Docker image (image builds
# kernels internally) or rebuild natively per vendor/FlashRT/README.md.
*.so
*.so.*
vendor/FlashRT/flash_rt/aarch64_backup/

# Model weights (large, download via scripts/download_checkpoint.sh)
ckpt/
checkpoints/
*.safetensors

# Logs / eval results
*.log
eval_result/
eval_summary/

# Editors / OS
.vscode/
.idea/
.DS_Store
*.swp
